# 협업 노트

> 프론트 개발자와 논의/공유가 필요한 항목 모음.

---

## 🔧 기술 · API

| 항목 | 내용 | 등록일 | 상태 |
|------|------|--------|------|
| 지역 입력 드롭다운 | region/district 입력 폼을 자유 텍스트 대신 드롭다운으로 변경 요청 — DB 데이터 일관성 보장 목적 | 2026-04-04 | 전달 필요 |
| Place ID 방식 변경 | 현재 프론트는 slug 기반 ID 사용 중. 백엔드 API는 BIGINT 반환 예정 — 프론트 연동 시 ID 참조 방식 변경 필요 | 2026-04-04 | 논의 필요 |
| 카테고리 복수 표시 | 장소가 여러 카테고리를 가질 수 있음. 현재 UI는 배지 하나만 표시 — 상세 화면, 카드, 필터 모두 복수 카테고리 대응 필요 | 2026-04-05 | 전달 필요 |
| 브랜드 입력 방식 변경 | 제보 폼의 브랜드 입력을 자유 텍스트 → 드롭다운 + 검색 + 없으면 추가 방식으로 변경 요청 — 동일 브랜드 오타/중복 방지 (이사거/ISAGER/이사게르 등) | 2026-04-05 | 전달 필요 |
| 주소 검색 시 좌표 전송 | 제보 폼에서 카카오 우편번호 API로 주소 선택 후, 카카오 geocoding API로 lat/lng 변환해서 백엔드에 함께 전송 — 백엔드가 외부 API 의존성 없이 좌표 저장 가능 | 2026-04-05 | 전달 필요 |
| 기존 장소 업데이트 제보 시 현재 정보 프리필 | "기존 장소 업데이트" 선택 후 장소 검색 시 현재 DB 값을 필드에 미리 채워줘야 함 — 사용자가 변경 전/후 비교하며 입력 가능. 장소 선택 시 API 호출 필요 | 2026-04-05 | 전달 필요 |
| API 응답 포맷 변경 | 모든 정상 응답은 `{ "data": T }` 래퍼로 반환. 에러는 `{ "code": "...", "status": 404, "message": "..." }`. 프론트 API 클라이언트 공통 처리 필요 | 2026-04-05 | 전달 필요 |
| 제보 API 경로 변경 | 제보 기능명 submission → request 변경. `POST /api/requests/places`, `POST /api/requests/events` | 2026-04-05 | 전달 필요 |

## 🎨 기획 · UX

| 항목 | 내용 | 등록일 | 상태 |
|------|------|--------|------|

## ❓ 함께 고민할 것

| 항목 | 내용 | 등록일 | 상태 |
|------|------|--------|------|
| 이벤트에 카테고리/태그/브랜드 붙을 가능성 | 현재 백엔드 구조: Category·Tag·Brand는 Place 전용으로 설계됨. Event는 Place에만 연결되고 카테고리/태그/브랜드 없음. 나중에 "이벤트를 카테고리/태그로 필터"하거나 "이벤트에 브랜드 태그"하는 기능이 생길 가능성 있는지 확인 필요. 없으면 현 구조 유지, 있으면 공용 도메인으로 분리 필요 | 2026-04-06 | 논의 필요 |
